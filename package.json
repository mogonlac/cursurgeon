{
  "name": "cursurgeon",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "bundle-extension": "npm run build && node extension/prep-media.mjs",
    "compile-extension": "npm run compile --prefix extension",
    "install:extension": "npm install --prefix extension",
    "setup": "npm install && npm install --prefix extension && npm run compile-extension && npm run bundle-extension",
    "vscode:prepublish": "npm run bundle-extension && npm run compile-extension",
    "package-vsix": "npm run bundle-extension && npm run compile-extension && cd extension && npx --yes @vscode/vsce package --no-dependencies"
  },
  "dependencies": {
    "html2canvas": "^1.4.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "typescript": "~5.6.2",
    "vite": "^5.4.10"
  }
}
